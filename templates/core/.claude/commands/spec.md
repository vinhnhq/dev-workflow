---
description: Start spec-driven development — write a structured specification before writing code
---

Invoke the `agent-skills:spec-driven-development` skill.

Clarify what the user wants to build:
1. Objective and target users
2. Core features and acceptance criteria (specific, testable, binary)
3. Tech stack and constraints
4. Boundaries (always do, ask first about, never do)

Produce a spec covering: context, what we're building, acceptance criteria, device/viewport matrix (per `dev-workflow.md` Phase 1), out of scope.

Save as `__project__/tasks/sprint-NN-name/plan.md`. Confirm with the user before proceeding to Phase 2.
