// Compute and render a colorized diff between two files.
// Used by the upgrade command.
//
// TODO (next session): implement. Suggest using `diff` from node:util or a
// minimal hand-rolled line-diff. Avoid heavy deps — keep `bunx` install fast.

export async function diffFiles(_localPath, _templatePath) {
	throw new Error("diffFiles: not yet implemented");
}
