// Recursive copy from a template dir into a target dir.
// Used by both init and add-preset commands.
//
// TODO (next session): implement.

export async function copyTemplates(_sourceDir, _targetDir, _options = {}) {
	throw new Error("copyTemplates: not yet implemented");
}
